<div id='pager'>
  <% if (page.count > 1) { %>
    <% if (page.number) { %> <!-- See if there is a page.number-->
      <a id='prev' href='/<%= page.number %>'>Prev</a>
    <% } %>
    <% if (page.number < page.count - 1) { %>
      <% if (page.number) { %>
         &nbsp;&nbsp;
      <% } %>
      <a id='next' href='/<%= page.number + 2 %>'>Next</a>
    <% } %>
  <% } %>
</div>
